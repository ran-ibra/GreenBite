# .env.example
# Copy this file to `.env` and fill in real values for your local environment.
# DO NOT commit your real `.env` file — it contains secrets.

# Django secret key (keep this secret in production)
SECRET_KEY=replace-with-a-long-random-string

# Debug mode (True for local development, False in production)
DEBUG=True

# Database configuration (example values)
# For SQLite, you can keep the defaults or use DB engine and connection details for Postgres/MySQL
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3
DATABASE_USER=your_db_user
DATABASE_PASS=your_db_password
DATABASE_HOST=localhost
DATABASE_PORT=5432

# Hosts allowed to serve the app (space-separated). Example: 127.0.0.1 localhost
ALLOWED_HOSTS="127.0.0.1 localhost"

# Optional: email or external service credentials (placeholders)
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=you@example.com
# EMAIL_HOST_PASSWORD=supersecret

# Notes for the team:
# - Keep the real `.env` out of git. Use secrets management for production (Vault, GitHub Secrets, etc.).
# - Add any new runtime config here as KEY=VALUE pairs and update `.env.example` accordingly for onboarding.
